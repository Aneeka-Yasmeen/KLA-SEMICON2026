numpy==2.5.2
torch==2.13.0

# Install the CUDA build matching the target machine's GPU driver from
# https://pytorch.org/get-started/locally/ if the default `pip install torch` does not pick up
# GPU support, e.g.:
# pip install torch==2.13.0 --index-url https://download.pytorch.org/whl/cu126
